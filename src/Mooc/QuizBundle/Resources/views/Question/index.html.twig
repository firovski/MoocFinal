{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Question list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Questiontext</th>
                <th>Type</th>
                <th>Point</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('question_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.questiontext }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.point }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('question_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('question_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                    <li>
                        <a href="{{ path('reponse_list', { 'id':entity.id }) }}">Reponse</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('question_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
