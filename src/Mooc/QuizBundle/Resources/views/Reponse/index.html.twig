{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Reponse list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reponsetext</th>
                <th>Reponsecorrecttext</th>
                <th>Reponsecorrectradio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('reponse_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.reponsetext }}</td>
                <td>{{ entity.reponsecorrecttext }}</td>
                <td>{{ entity.reponsecorrectradio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('reponse_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('reponse_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('reponse_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
